<Window x:Class="HCIproject.FindFileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HCIproject"
                xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="FindFileWindow" Height="400" Width="700"
        WindowStartupLocation="CenterScreen">
    <StackPanel x:Name="Resize" Orientation="Vertical" Background="#D0ECE7" >
        <StackPanel>
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300"  >
                    <Grid x:Name="Grid_Grid1" Margin="0,0,0,32"/>
                </ScrollViewer>
            </Grid>
        </StackPanel>
        <Button x:Name="logBtn"
                        Style="{StaticResource MaterialDesignRaisedButton}" materialDesign:ButtonAssist.CornerRadius="10"  ToolTip="MaterialDesignRaisedButton with Round Corners"
                        Content="Povratak"  FontSize="17" HorizontalAlignment="Right" Height="26" Background="#FFA2D9CE" BorderBrush="#FF1FEECD" Click="logBtn_Click" />

    </StackPanel>
</Window>
